#include <stdio.h>

main(){

    //area of rectangle
    float length,width;
    printf("\n enter the length: ");
    scanf("%f", &length);
    printf("\n enter the width:");
    scanf("%f", &width);
    printf("\n The area of the rectangle is %.2f" , length * width);

}

